version: 0.2

phases:
  install:
    commands:
      - echo Installing
      - pip install -r requirements.txt || true

  build:
    commands:
      - echo Running app
      - python app.py || true

artifacts:
  files:
    - '**/*'
